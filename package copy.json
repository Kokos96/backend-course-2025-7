{
  "name": "backend-course-2025-6",
  "version": "1.0.0",
  "description": "Inventory service",
  "main": "index.js",
  "scripts": {
    "start": "node index.js --host localhost --port 3000 --cache ./cache",
    "dev": "nodemon --ignore cache/ index.js --host localhost --port 3000 --cache ./cache"
  },
  "dependencies": {
    "commander": "^11.1.0",
    "express": "^4.18.2",
    "multer": "^1.4.5-lts.1",
    "superagent": "^8.1.2",
    "swagger-ui-express": "^5.0.1"
  },
  "devDependencies": {
    "nodemon": "^3.0.1"
  }
}
